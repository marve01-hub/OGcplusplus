#include<iostream>
#include<vector>
using namespace std;
int main(){
vector<int> v;
    // v = {1,2,3,4,5};
    // vector<int>::iterator it = v.begin();
    // it++;
    // cout<< *(it) <<" "; //used pointer to accecss value

     vector<int>::iterator it = v.begin(); // points toeards memory location of 0th index

     
    vector<int>::iterator it = v.end(); // points toawards the memory loaction just after last element

}